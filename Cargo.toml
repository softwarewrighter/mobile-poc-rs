[package]
name = "mobile_poc_core"
version = "0.1.0"
edition = "2021"
authors = ["Mobile POC Team"]
description = "Core library for Rust Mobile Sensor POC"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
